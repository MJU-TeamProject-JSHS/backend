spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/dding
#    username: dding
#    password: ddingpw
#  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        format_sql: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        show_sql: true
  security:
    oauth2:
      client:
        registration:
          kakao: # 'kakao'라는 이름으로 등록
#            client-id: ${KAKAO_CLIENT_ID}
#            client-secret: ${KAKAO_CLIENT_SECRET}
#            client-id: 3f4f8f72459c099903bc9d4c7d7e5331
#            client-secret: j5X51YQh439nEua0cSKoXqDjdyMQAoRs
            client-id: b296a640468c8a343dafa325ad9bf16f
            client-secret: lgaqVAtRk4a7BsL1Kz54nRDfkBiy1zBU
#            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
#            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            redirect-uri: https://volubly-uncorroboratory-consuelo.ngrok-free.dev/login/oauth2/code/kakao
#            redirect-uri: "http://localhost:8080/api/user/my-info"
            scope:
              - profile_nickname
              - profile_image
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            client-name: kakao

        provider:
          kakao: # 'kakao' 제공자에 대한 정보
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id # 사용자 정보를 가져왔을 때 기준이 되는 key

  jwt:
    secret:
      key: 3QJqamO97ttPCbZhzJQ7uQGdBik5l/DbHelrxiREQdvf32imV7YrIncAtAKqw7iZ+OZSnJxR0V39n3vsvqr2MA==

logging:
  level:
    org.hibernate.SQL: debug

server:
  port: 8080
