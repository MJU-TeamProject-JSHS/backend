services:
  db:
    image: postgres:16
    container_name: dding-postgres
    environment:
      POSTGRES_DB: dding
      POSTGRES_USER: dding
      POSTGRES_PASSWORD: ddingpw
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
